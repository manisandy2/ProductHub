version: "3.9"

services:

  producthub:

    build: .

    container_name: producthub

    env_file:
      - .env

    volumes:
      - ./output:/app/output
      - ./logs:/app/logs

    restart: unless-stopped